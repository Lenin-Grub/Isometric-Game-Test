#pragma once

namespace smpl
{
    namespace Mouse
    {
        enum class Button
        {
            Left,
            Right,
            Middle,
            XButton1,
            XButton2,
        };

        enum class Scroll
        {
            Up,
            Down,
            Left,
            Right
        };
    }
}